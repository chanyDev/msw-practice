import Rest from '../components/Rest';
import Graphql from '../components/Graphql';

const Home = () => {
  return (
    <div style={{ textAlign: 'center' }}>
      <Rest />
      <Graphql />
    </div>
  );
};

export default Home;
